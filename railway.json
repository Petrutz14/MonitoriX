{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "monitor_pc/Dockerfile"
  },
  "deploy": {
    "startCommand": "java -jar app.jar --spring.profiles.active=prod",
    "restartPolicyType": "ON_FAILURE"
  }
}
